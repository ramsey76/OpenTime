# OpenTime
Timemanagement application desgined with OpenSpec


# OpenSpec Skills
You might need to do a OpenSpec init


# Install OpenSpec
You can install OpenSpec with the following command:

```bash
pip install openspec
```


