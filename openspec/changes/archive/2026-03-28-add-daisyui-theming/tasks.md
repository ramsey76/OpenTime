## 1. Install and Configure DaisyUI

- [x] 1.1 Install DaisyUI v5 as a dev dependency in `src/TimeManagement.Web/`
- [x] 1.2 Update `style.css` to register DaisyUI plugin with `corporate` theme via `@plugin` directive
- [x] 1.3 Set `data-theme="corporate"` on the root HTML element

## 2. Refactor Layout and Navigation

- [x] 2.1 Refactor `AppLayout.vue` sidebar to use DaisyUI `menu` classes and active styling

## 3. Refactor Shared Components

- [x] 3.1 Refactor `ToastNotification.vue` to use DaisyUI `toast` and `alert` classes
- [x] 3.2 Refactor `ConfirmDialog.vue` to use DaisyUI `modal` and `modal-box` classes with `btn` variants
- [x] 3.3 Refactor `RoleFormModal.vue` to use DaisyUI `modal`, `input`, `textarea`, and `btn` classes
- [x] 3.4 Refactor `DepartmentFormModal.vue` to use DaisyUI `modal`, `input`, `select`, and `btn` classes

## 4. Refactor Pages

- [x] 4.1 Refactor `RolesPage.vue` to use DaisyUI `table` and `btn` classes
- [x] 4.2 Refactor `DepartmentsPage.vue` to use DaisyUI `table` and `btn` classes

## 5. Verify

- [x] 5.1 Run `vite build` to confirm the project builds without errors
